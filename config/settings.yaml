seed: 0

model:
  input_dim: 20
  hidden_dim: 16
  output_dim: 5

data:
  batch_size: 10
  synthetic_inputs_path: "data/synthetic_inputs.npy"

weights:
  synced_weights_path: "weights/synced_weights.npz"

performance:
  num_runs: 100
